/**
 * Back-compat re-export. The canonical implementation now lives alongside the
 * other envelope-aware response decorators in `api-response.decorator.ts`.
 */
export { ApiPaginatedResponse } from './api-response.decorator';
